#include <stdio.h>

/**
 * main - Entry point
 *
 * void - does not accept args
 *
 * Return: always 0 (success)
 *
 */
int main(void)
{
	int i;
	int j;

			for (i = '0'; i <= '9'; i++)
			{
				for (j = '0'; j <= '9'; j++)
				{
					putchar(i);
					putchar(j);
					putchar(' ');
					putchar(k);
					putchar(l);

					putchar(',');
					putchar(' ');
				}
			}
	putchar('\n');
	return (0);
}
