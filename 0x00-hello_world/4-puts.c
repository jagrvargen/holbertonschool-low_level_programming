#include <stdio.h>

/**
*main - Entry point
*
*puts: prints text to standard output.
*
*Return: Always 0 (success)
*/
int main(void)
{
	int puts(const char *s);

	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
